schema_version: "2.0"

metadata:
  name: "DAG Test - Circular Dependency (Should Fail)"
  description: "This should fail with cycle detection"

parameters:
  a: ${b}
  b: ${a}

parts:
  base:
    primitive: box
    width: 100
    depth: 100
    height: 20
